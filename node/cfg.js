var config = {
  key: {
    serviceMail: process.env.RIEMON_SERV_MAIL,
    usuMail: process.env.RIEMON_USU_MAIL,
    passMail: process.env.RIEMON_PASS_MAIL,
    fromMail: process.env.RIEMON_FROM_MAIL,
    toMail: process.env.RIEMON_TO_MAIL
  }
}

module.exports = config